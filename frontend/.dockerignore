# Dependencies and build output are produced inside the image.
node_modules/
dist/
coverage/

# Local environment overrides must never leak into an image.
.env
.env.*
!.env.example

Dockerfile
.dockerignore
*.md

.idea/
.vscode/
.DS_Store
Thumbs.db
